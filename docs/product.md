# Product notes

Nimbus Board e um console operacional para o dominio **logistica / tracking**.

KPIs simulados: volume, SLA, itens em risco e distribuicao de status
`created / in_transit / delivered`.
